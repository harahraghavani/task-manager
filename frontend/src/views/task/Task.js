import React from 'react'
import Layout from '../../components/layout/Layout'
import CommonButton from '../../components/commmon/CommonButton'

const Task = () => {
    return (
        <Layout>
            <CommonButton btnText={"Create Task"} />
        </Layout>
    )
}

export default Task